version: "2"

sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./db/queries/"
    gen:
      go:
        package: "db"
        out: "./internal/db"
        emit_json_tags: true
        emit_interface: true